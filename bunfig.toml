[install]
# Use bun.lockb lockfile
lockfile = true

# Handle peer dependencies
peer = true

[install.cache]
# Enable global cache for faster installs
disable = false

[test]
# Use Bun's test runner
preload = []
